"""
OHLCSniper — PAT-5: CNN-LSTM pattern detection model removed.

This module is retained as a stub so existing imports do not break.
The model was disconnected in Phase 0B and fully removed in PAT-1 through PAT-7.
"""
from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


class OHLCSniper:
    """PAT-5: stub — pattern detection module removed."""

    def __init__(self, *args, **kwargs):
        self.model = None
        logger.info("[OHLCSniper] Pattern detection module disabled (PAT-5 stub).")

    def predict(self, *args, **kwargs):
        return None

    def load_model(self, *args, **kwargs):
        pass
